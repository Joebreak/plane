# LINE 管理者後台（repo: plane）

## 開發

```bash
cp .env.example .env.local
npm install
npm run dev
```

本機請保持 `VITE_BASE_PATH=/`

## 建置

```bash
npm run build
```

GitHub Pages 網址：`https://<你的帳號>.github.io/plane/`  
Actions 預設 `VITE_BASE_PATH=/plane/`
